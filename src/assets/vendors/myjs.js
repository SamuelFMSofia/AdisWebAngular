/* var e=document.getElementById("name").addEventListener('keyup', autoCompleteNew);

function autoCompleteNew(e) {
    var value = $(this).val();
    $("#short-name").val(value.replace(/\s/g, '').toLowerCase());
}
 */
